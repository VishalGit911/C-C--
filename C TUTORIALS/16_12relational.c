#include <stdio.h>
int main()
{ // this code is for relatio0n operator or comparison operator....
    int a, b;
    printf("enter number of a :");
    scanf("%d", &a);
    printf("enter number of b :");
    scanf("%d", &b);
    printf("the both number are same %d\n", a == b);
    printf("the both number is not same %d\n", a != b);
    printf("the number is bigger than opther %d\n", a > b);
    printf("the number is smaller than other numbr %d\n", a < b);
    printf("the number is greater than qual %d\n", a >= b);
    printf("the number is less than qual %d\n",a<=b);
}