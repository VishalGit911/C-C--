#include <stdio.h>
void main()
{
    // FOR THE TABLE

    int A;
    int table = 1;
    printf("Enter the value of A =");
    scanf("%d", &A);

    printf("%d * %d = %d\n", A, table, table * A);
    table++;
    printf("%d * %d = %d\n", A, table, table * A);
    table++;
    printf("%d * %d = %d\n", A, table, table * A);
    table++;
    printf("%d * %d = %d\n", A, table, table * A);
    table++;
    printf("%d * %d = %d\n", A, table, table * A);
    table++;
    printf("%d * %d = %d\n", A, table, table * A);
    table++;
    printf("%d * %d = %d\n", A, table, table * A);
    table++;
    printf("%d * %d = %d\n", A, table, table * A);
    table++;
    printf("%d * %d = %d\n", A, table, table * A);
    table++;
    printf("%d * %d = %d\n", A, table, table * A);
    table++;
}